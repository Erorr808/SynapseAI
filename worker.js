'use strict';

/**
 * Web worker entry (placeholder).
 * You can move heavy computation here to keep the UI responsive.
 */

// self.onmessage = (event) => {
//   const { taskId, payload } = event.data;
//   // TODO: perform heavy work here
//   self.postMessage({ taskId, result: payload });
// };

